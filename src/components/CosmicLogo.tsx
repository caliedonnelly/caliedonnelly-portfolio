"use client";
import { useRef, useEffect } from "react";

export function CosmicLogo() {
  const canvasRef = useRef<HTMLCanvasElement>(null);

  useEffect(() => {
    // Paste the entire rainbow comet script here (same as before)
    // I'll give you a clean version in a sec
  }, []);

  return <div className="w-24 h-24">/* SVG + Canvas */</div>;
}
